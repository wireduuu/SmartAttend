export const ACCESS_TOKEN_LIFETIME_SECONDS = 15 * 60; // 15 minutes
export const SESSION_WARNING_SECONDS = 5 * 60; // warn 5 minutes before expiry

export const IDLE_TIMEOUT_SECONDS = 10 * 60; // 10 minutes idle logout
export const IDLE_WARNING_SECONDS = 2 * 60; // warn 2 minutes before idle logout
